{
  "name": "app",
  "version": "1.0.0",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "next": "^14.2.32",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "tippy.js": "^6.3.7",
    "use-query-params": "^2.2.1",
    "@rainbow-me/rainbowkit": "^2.2.4",
    "react-toastify": "^9.1.1"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  },
  "scripts": {
    "predev": "yarn workspace arb-token-bridge-ui generateDenylist && yarn workspace arb-token-bridge-ui css:build & yarn workspace portal css:build",
    "dev": "next dev",
    "prebuild": "yarn workspace arb-token-bridge-ui generateDenylist && yarn workspace arb-token-bridge-ui css:build & yarn workspace portal css:build",
    "build": "next build",
    "start": "next start",
    "lint": "tsc && eslint",
    "lint:fix": "tsc && eslint --quiet --fix",
    "css:build": "yarn workspace arb-token-bridge-ui css:build & yarn workspace portal css:build",
    "css:watch": "yarn workspace arb-token-bridge-ui css:watch & yarn workspace portal css:watch"
  }
}
